# PlatformIO
firmware/.pio/
firmware/include/secrets.h

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Mosquitto — credenciais reais, nunca versionar (só passwd, não os .conf)
infra/mosquitto/config/passwd

# InfluxDB — credenciais reais (senha + token de admin), nunca versionar
infra/.env

# LaTeX (docs/) — arquivos auxiliares de compilação, só o .tex/.pdf são versionados
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.fls
docs/*.fdb_latexmk

# OS / editor
.DS_Store
*.swp
